server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Vue Router (history mode): serve index.html for client-side routes on reload.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
