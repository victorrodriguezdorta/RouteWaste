# API base for Playwright e2e (isolated api-test back-end; trailing slash required).
# Dev UI uses .env.development (8080 / db-application); e2e uses 8081 / db-application-api-test.
VITE_APP_API_URL=http://localhost:8081/api/v1/
