## Configuration for 'mongo' Spring profile
# Use an environment variable for the URI to avoid committing credentials.
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/sensor-app}
spring.data.mongodb.database=sensor-app
logging.level.org.springframework.data.mongodb=DEBUG
