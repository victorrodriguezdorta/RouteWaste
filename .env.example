# GitHub PAT with read:packages (required to build front-end and back-end images).
# 1. Create one at https://github.com/settings/tokens/new (scope: read:packages)
# 2. Copy this file:  cp .env.example .env
# 3. Paste your token below (Docker Compose reads .env automatically)
GITHUB_TOKEN=
